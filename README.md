Day 1 — 19 March 2026

Solved 4 problems today as part of the DSA prep in Python.

The problems covered were Two Sum (HashMap), Valid Palindrome (string cleaning), Best Time to Buy and Sell Stock (greedy single pass), and Contains Duplicate (HashSet).

Learned that dictionaries and sets both give O(1) lookup, which makes them the go-to tool for "have I seen this before?" problems. Also understood why if-elif is used instead of two separate if statements when two conditions are mutually exclusive — elif only runs when the first if is false.
